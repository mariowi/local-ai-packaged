# n8n Workflows Configuration
# Copy this file to config.env and fill in your values

# n8n server (IP:PORT)
N8N_HOST=192.168.1.100:5678

# n8n API key (Settings > API > Create API Key)
N8N_API_KEY=your_n8n_api_key_here

# CAAL webhook server (IP:PORT) - for reload-tools and announce endpoints
CAAL_HOST=192.168.1.100:8889

# Radarr server (IP:PORT) - only needed if using radarr_search_movies workflow
RADARR_HOST=192.168.1.100:7878
